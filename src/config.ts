const API_SERVER = 'localhost';
const API_PORT = 3000;
const PROTOCOL = 'http';
export const APOLLO_URL = `${PROTOCOL}://${API_SERVER}:${API_PORT}/graphql`;
export const TEST_API_URL = `${PROTOCOL}://${API_SERVER}:${API_PORT}/graphql`;
export const AUTH_URL = `${PROTOCOL}://${API_SERVER}:${API_PORT}/auth`;
export const TEST_AUTH_URL = `${PROTOCOL}://${API_SERVER}:${API_PORT}/auth`;
